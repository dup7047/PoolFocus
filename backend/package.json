{
  "name": "poolfocus-backend-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node dist/test/scoring.test.js",
    "dev": "tsc -p tsconfig.json && node dist/src/server.js"
  },
  "devDependencies": {}
}
